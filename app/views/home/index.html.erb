<%= content_tag :div,
  id: "home-index-data",
  data: { osers: @osers, comment_count: @comment_count, comments: @comments, current_oser: current_oser.as_json(only: [:id, :flair, :username]), logged_in: logged_in? } do %>
<% end %>

<%= javascript_pack_tag 'components/AdminDashboard' %>
